package nemotasks.service;

import nemotasks.Main;
import nemotasks.entity.Task;

public class TaskFabric {

    /*public static void createTask(String description) {
        Task task = new Task(Main.getDb().getTaskCount() + 1 , description);
        Main.getDb().addTask(task);
    }*/
}
